import { Request, Response } from "../../type";

class AdminController {
    // Admin controller methods have been removed as per requirements
    // This class is kept for potential future admin functionality
}

export default new AdminController();